name: Checking system configaration

on: workflow_dispatch

jobs:
    System-config:

        runs-on: ubuntu-latest

        steps:

            - name: Linux commands
              run: pwd

            - name: Linux commands
              run: ls -lrth

            - name: create a file
              run: echo "My Name is Dhage Anil Kumar and Im learning Github Actions" > anilkumar.txt

            - name: Linux commands
              run: date